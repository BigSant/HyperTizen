<Project Sdk="Tizen.NET.Sdk/1.1.9">

  <!-- Property Group for .NET Core Project -->
  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>tizen90</TargetFramework>
    <AllowUnsafeBlocks>true</AllowUnsafeBlocks>
  </PropertyGroup>

  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Debug|AnyCPU' ">
    <DebugType>portable</DebugType>
  </PropertyGroup>
  <PropertyGroup Condition=" '$(Configuration)|$(Platform)' == 'Release|AnyCPU' ">
    <DebugType>None</DebugType>
  </PropertyGroup>

  <ItemGroup>
    <Folder Include="lib\" />
    <Folder Include="res\" />
  </ItemGroup>

  <!-- TV builds are control-only. Full-frame processing runs in the PC/WSL
       source bridge, so no capture probes or native capture interop are
       compiled into the package. -->
  <ItemGroup>
    <Compile Remove="HyperionClient.cs" />
    <Compile Remove="DiagnosticCapture.cs" />
    <Compile Remove="VideoCapture.cs" />
    <Compile Remove="SDK\SecVideoCapture.cs" />
    <Compile Remove="Capture\**\*.cs" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Google.FlatBuffers" Version="25.2.10" />
    <PackageReference Include="Newtonsoft.Json" Version="13.0.3" />
    <PackageReference Include="Rssdp" Version="4.0.4" />
    <PackageReference Include="SkiaSharp" Version="2.88.9" />
    <PackageReference Include="Tizen.NET.TV" Version="6.6.0.5599517" />
  </ItemGroup>

</Project>
